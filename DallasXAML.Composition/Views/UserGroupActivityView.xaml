<UserControl
    x:Class="DallasXAML.Composition.Views.UserGroupActivityView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    d:DesignHeight="82"
    d:DesignWidth="277">
    <Border Margin="5" Background="#E95858">
        <Grid Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition
                    Height="Auto" />
                <RowDefinition
                    Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock
                Text="{Binding Person}"
                FontSize="26.667" />
            <TextBlock
                Text="{Binding Attendees}"
                Grid.Row="1"
                FontSize="20" Margin="0" />
        </Grid>
    </Border>
</UserControl>
